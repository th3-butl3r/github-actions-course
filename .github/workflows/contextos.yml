name: Contexto
run-name: ¡Estoy aprendiendo a usar Contextos!
on: push
jobs:
  check-main:
    if: ${{ github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    steps:
      - name: Revision contexto
        uses: actions/checkout@v4
        run: echo "Desplegando en la rama $GITHUB_REF"
